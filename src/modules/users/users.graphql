type Query {
  getUserById(id: String): [User]
}

type User {
  id: String
  email: String
  firstName: String
  lastName: String
  phoneNumber: String
  adress: UserAdress
}

type UserAdress { 
  country: String
  region: String
  city: String
  postcode: String
  street: String
  house: String
  flat: String
}